node_modules
.next
out
.env*
!.env.example
next-env.d.ts
*.tsbuildinfo
.DS_Store

# raw screenshot sources (converted into public/screens)
.design-src/

# raw screenshot drops (convert into public/screens before use)
captain/
client/

# raw screenshot sources (converted into public/screens)
assets/
